[project]
name = "docker-agent"
version = "0.1.0"
description = "A tiny local agent template using SGLang"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
   "openai>=1.20.0",
   "rich>=13.7.1",
]
